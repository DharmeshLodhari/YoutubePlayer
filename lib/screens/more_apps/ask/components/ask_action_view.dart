import 'package:flutter/material.dart';

class AskActionView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(children: [

      ]),
    );
  }
}