image: cirrusci/flutter:latest

stages:
  - build

before_script:
  - flutter pub get
  - flutter clean


build:
  stage: build
  script:
    - flutter build apk

  artifacts:
    paths:
      - build/app/outputs/apk/release/app-release.apk

  tags:
    - flutter

